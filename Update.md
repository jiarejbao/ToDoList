##### 0418
根据以下要求更改README.md:
1、该网站具体功能要求有：
>网站包含主页面面和任务页面：
>主页面展示任务列表，该页面只展示每个任务的基本信息如创建日期，截止日期，优先级，描述等，可以设计图标/列表等多种展示格式；
>点击主页面的具体任务之后进入该任务的任务页面：
>任务页面的主体是该任务下辖的各个子任务卡片，卡片中只展示每个子任务的基本信息如创建日期，截止日期，优先级，描述等，以图形式展示子任务之间的工作流(这里需要联网搜索目前图形式展示工作流的主流方式与我交互确认)，同时任务页面有Note功能，可以在侧边栏打开/关闭，打开后可以进行执行笔记功能(这里需要联网搜索目前页面嵌入笔记的主流方式与我交互确认)；
>点击子任务卡片可以进入该卡片的note页面。
2、理解上述任务之后确定各个环节的技术栈（在当前技术栈基础上完善）；
3、理解上述任务之后设计API接口（目前的API接口远远不够完善）；
4、基于API接口设计重构数据库设计，最后在README.md中展示数据库的ER图和表结构设计；
5、基于上述设计，重构当前目录结构以及各文件中各接口的具体实现方式（当前很多接口没有实现方式）
6、最后修改后的README.md应当包含以下结构：任务要求，整体目标，技术架构（技术栈，项目目录结构），数据存储架构（包含原本数据存储结构和数据模型的内容，具体小章节可自由发挥），API接口设计（具体各接口的设计和实现要有整体性，可自由发挥），设计风格，运行方式，License.
